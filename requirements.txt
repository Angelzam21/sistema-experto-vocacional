# =========================================================
# DEPENDENCIAS DEL PROYECTO - SISTEMA EXPERTO VOCACIONAL
# =========================================================
# Este archivo lista todos los paquetes Python necesarios para
# ejecutar tanto la fase ETL (offline) como la app Streamlit.
# Instalación: pip install -r requirements.txt
# =========================================================

# --- Framework principal de la app web ---
streamlit>=1.31.0

# --- Cálculo numérico y matricial ---
# NumPy es la base; se usa para vectores RIASEC y similitud de coseno.
numpy>=1.26.0

# --- Visualización del Radar Chart (Fase 4.3) ---
plotly>=5.18.0

# --- ETL: Web Scraping (Fase 1.1) ---
# requests + BeautifulSoup cubren páginas HTML estáticas.
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- ETL: Normalización (Fase 1.2) ---
# rapidfuzz reemplaza a fuzzywuzzy: misma API, mucho más rápido y sin licencia GPL.
rapidfuzz>=3.6.0

# --- Manejo de datos tabulares (opcional pero útil para ETL) ---
pandas>=2.1.0
